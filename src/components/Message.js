import styled from "styled-components"

const Message = () => {
    return (
        <MessageContainer>

        </MessageContainer>
    )
}

export default Message

const MessageContainer = styled.div`

`